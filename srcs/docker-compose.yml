
services:
  nginx:
    container_name: nginx
    hostname: nginx 
    build:
      context: ./requirements/nginx
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      - wordpress
    ports:
      - "443:443"
    volumes:
      - wordpress_volume:/var/www/html
    networks:
      - inception_network

  wordpress:
    container_name: wordpress
    hostname: wordpress 
    build:
      context: ./requirements/wordpress
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      - mariadb
    volumes:
      - wordpress_volume:/var/www/html
    networks:
      - inception_network
    env_file:
      - .env

  mariadb:
    container_name: mariadb
    hostname: mariadb
    build:
      context: ./requirements/mariadb
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - mariadb_volume:/var/lib/mysql
    networks:
      - inception_network
    env_file:
      - .env

  adminer:
    container_name: adminer
    hostname: adminer
    build:
      context: ./requirements/adminer
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      8080:8080
    networks:
      - inception_network
    env_file:
      - .env

networks:
  inception_network:

volumes:
  wordpress_volume:
    driver: local
    driver_opts:
      type: none
      device: /home/sneshev/data/wordpress
      o: bind
  mariadb_volume:
    driver: local
    driver_opts:
      type: none
      device: /home/sneshev/data/mariadb
      o: bind